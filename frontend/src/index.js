import React from "react";
import ReactDOM from "react-dom/client";
import "./styles/Login.css"; // Estilos globales
import "./styles/Home.css"; // Estilos para Home
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
